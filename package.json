{
  "name": "postgres-read",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun build --compile --minify --sourcemap ./index.ts --outfile postgres-read",
    "test": "bun run test_verification.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0"
  },
  "release": {
    "branches": [
      "main"
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      [
        "@semantic-release/github",
        {
          "assets": [
            {
              "path": "postgres-read",
              "name": "postgres-read-macos-arm64",
              "label": "postgres-read-macos-arm64"
            }
          ]
        }
      ]
    ]
  }
}